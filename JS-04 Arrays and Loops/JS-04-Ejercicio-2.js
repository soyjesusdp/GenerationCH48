//?=====================================Exercise 2.0
//!Number divided into halves

function numDiv(number) {
    let segundopaso = number / 2;
    let result = [segundopaso, segundopaso];
    return result;
}
console.log(numDiv(4));

//?=====================================Exercise 2.1
//!Number divided into halves

function numDivision(number) {
    let numDivisionTotal = number / 2;
    let result = [numDivisionTotal, numDivisionTotal];
    return result;
}
console.log(numDivision(10));